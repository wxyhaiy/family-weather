# 项目操作规则

- 当用户说“运行一次”“执行一次”或“让我看下效果”时，默认指通过 GitHub Actions 手动触发对应 workflow，而不是在本地运行 Python 程序。
- 执行前应先确认将要触发的 GitHub Actions workflow；除非用户明确要求，本地运行仅用于诊断，不视为完成“运行一次”。
- 用户明确说“先不动手”时，只记录或说明计划，不触发 workflow，也不发送实际微信消息。
